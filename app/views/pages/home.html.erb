<div class="space"></div>

<% if user_signed_in? %>

		<%= link_to "Create a Company", new_company_url(current_user) %>
		<%= link_to "Connect to a Company", new_user_employment_url(current_user) %>
	

	<% if current_user.employed? %>
		<%= link_to "Create a Leave Request",
			new_user_employment_leave_request_url(current_user, @employment.id)%>

	<% end %>

		<%= link_to "Become an HR Officer" %>
<% end %>