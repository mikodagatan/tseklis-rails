<div class="space"></div>

<% if user_signed_in? %>

		<%= link_to "Create a Company", new_company_url %>
		<%= link_to "Connect to a Company", edit_user_employments_url(current_user) %>

	<% if current_user.employed? %>
	<% else %>
	<% end %>
<% end %>